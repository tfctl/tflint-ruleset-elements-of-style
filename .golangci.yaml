version: "2"
run:
  tests: true
linters:
  enable:
    - copyloopvar
    - dupl
    - gocritic
    - gocyclo
    - godot
    - gosec
    - misspell
    - predeclared
    - revive
    - unparam
  settings:
    gocyclo:
      min-complexity: 15
    godot:
      scope: all
      period: false
    revive:
      rules:
        - name: dot-imports
        - name: exported
        - name: var-naming
        - name: receiver-naming
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    paths:
      - .*_test\.go$
      - .*testdata/.*
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/tfctl/tflint-ruleset-elements-of-style
  exclusions:
    generated: lax
    paths:
      - .*_test\.go$
      - .*testdata/.*
      - third_party$
      - builtin$
      - examples$
